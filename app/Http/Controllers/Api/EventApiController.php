<?php

// EventApiController removed — placeholder file kept to avoid accidental autoload errors.
// The API routes for events were intentionally removed from routes/api.php to prevent
// exposing /api/events. If you need to restore the API later, recreate this controller.

namespace App\Http\Controllers\Api;

class EventApiController
{
    // intentionally empty
}
